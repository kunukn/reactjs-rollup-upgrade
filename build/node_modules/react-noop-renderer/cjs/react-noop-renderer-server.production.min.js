/** @license React vundefined
 * react-noop-renderer-server.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */

"use strict";
var ReactNoopServer = require("react-stream")({
    scheduleWork: function(callback) {
      callback();
    },
    beginWriting: function() {},
    writeChunk: function(destination, buffer) {
      destination.push(JSON.parse(Buffer.from(buffer).toString("utf8")));
    },
    completeWriting: function() {},
    close: function() {},
    flushBuffered: function() {},
    convertStringToBuffer: function(content) {
      return Buffer.from(content, "utf8");
    },
    formatChunk: function(type, props) {
      return Buffer.from(JSON.stringify({ type: type, props: props }), "utf8");
    }
  }),
  ReactNoopServer$1 = {
    render: function(children) {
      var destination = [];
      children = ReactNoopServer.createRequest(children, destination);
      ReactNoopServer.startWork(children);
      return destination;
    }
  },
  ReactNoopServer$2 = { default: ReactNoopServer$1 },
  ReactNoopServer$3 =
    (ReactNoopServer$2 && ReactNoopServer$1) || ReactNoopServer$2;
module.exports = ReactNoopServer$3.default || ReactNoopServer$3;
